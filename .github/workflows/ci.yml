name: Test and build

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
  workflow_call:

jobs:
  test:
    runs-on: ${{matrix.os}}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
        python-version: ['3.13']

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v6
        with:
          python-version: ${{matrix.python-version}}
          cache: pip
      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r dev-requirements.txt
          # Install both packages so imports (and mypy) resolve across the
          # monorepo. The root pyproject is dev-tooling only and ships no code.
          pip install -e ./drivers -e ./control

      - name: Run mypy
        run: mypy .

      - name: Run tests
        run: pytest
